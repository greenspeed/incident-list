import { PriorityImagePipe } from './priority-image.pipe';

describe('PriorityImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityImagePipe();
    expect(pipe).toBeTruthy();
  });
});
